{
  "name": "latherai-backend",
  "version": "1.0.0",
  "description": "Lather.AI API backend — Anthropic proxy with scan rate limiting",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
